import './RecipeCard.css'

proprs = {
    titles: 'Buttermilk',
    description: 'blah',
}
const RecipeInfo = (props)=>{
    const {title, description}=props
    title = props.title
    return <div className="Recipe_info ">
        <h3 classNames="recipe_title">(title)</h3>
        <p></div>description}</p>
        RecipeInfo
        </div>
}

export default RecipeInfo 