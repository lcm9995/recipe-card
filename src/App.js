const App = () => {
    return (
        <><main>
            <div>App</div>
        </main></>
    )
}
export default App 

//#whenever we say export default, we don't need curly braces when we import that component 